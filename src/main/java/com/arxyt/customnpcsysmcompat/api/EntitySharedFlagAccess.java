package com.arxyt.customnpcsysmcompat.api;

/** Stable non-mixin interface injected into vanilla Entity for shared-flag access. */
public interface EntitySharedFlagAccess {
    boolean customnpcsYsmCompat$getSharedFlag(int flag);
}
